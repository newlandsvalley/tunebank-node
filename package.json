{
  "name": "tunebank-node",
  "version": "0.0.1",
  "description": "web server for ABC notation music",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "server": "spago run -p server",
    "migration": "spago run -p migration -m Migrate",
    "bundle-server": "spago bundle -p server --bundle-type app --platform node --outfile tunebank.js"
  },
  "dependencies": {
    "esbuild": "^0.25.4",
    "js-yaml": "^4.1.0",
    "nodemailer": "^6.10.1",
    "pg": "^8.14.1"
  },
  "devDependencies": {
    "purescript": "^0.15.15",
    "spago": "^0.93.42"
  }
}
